\chapter*{Лекция 3.}

Продолжим \emph{вывод условия Вейерштрасса}. Имеем
\begin{equation}\label{3m23}
\int\limits_{\cal M_\varepsilon} \delta_u F\, dt =
\int\limits_{\cal M_{\varepsilon -}} \delta_u F\,dt +
\int\limits_{\cal M_{\varepsilon +}} \delta_u F\,dt,
\end{equation}
где
$$
{\cal M}_{\varepsilon -} = (\tau  - \varepsilon, \tau), \qquad
{\cal M}_{\varepsilon +} = (\tau, \tau + \sqrt{\varepsilon}).
$$
Рассмотрим каждое слагаемое в отдельности. Имеем:
\begin{equation}\label{3m24}
\int\limits_{\cal M_{\varepsilon -}} \delta_u F\, dt =
\int\limits_{\cal M_{\varepsilon -}} \Delta F\, dt +
\int\limits_{\cal M_{\varepsilon -}} (\delta_u F - \Delta F)\, dt
= \Delta F \cdot \varepsilon + o(\varepsilon),
\end{equation}
поскольку  mes ${\cal M}_{\varepsilon -} = \varepsilon$ , а
$\delta_u F - \Delta F$ можно переписать как
$$
F(t,x^0(t), u^0(t) + \Delta u) -
F(\tau,x^0(\tau), u^0(\tau) + \Delta u) - (F(t,x^0(t), u^0(t)) -
F(\tau, x^0(\tau), u^0(\tau))),
$$
откуда ясно, что
$$
\sup_{\cal M_{\varepsilon -}} |\delta_u F - \Delta F| \rightarrow
0 \mbox{  при  } \varepsilon\to 0,
$$
поскольку $x^\circ$ и $u^\circ$  непрерывны в точке  $\tau$.

Далее, покажем, что
\begin{equation}\label{3m25}
\int\limits_{\cal M_{\varepsilon +}} \delta_u F\,dt =
\int\limits_{\cal M_{\varepsilon +}} F_u^\circ \delta u\,dt  +
o(\varepsilon),
\end{equation}
где $F^\circ_u = F_u(t,
w^\circ(t))$.

Действительно, по теореме о среднем на ${\cal M}_{\varepsilon
+}$  имеем:
$$
\delta_u F = F_u^\theta \delta u,
$$
где
$$
F_u^\theta = F_u(t,x^\circ,u^\circ + \theta \delta u),\qquad
\theta =\theta(t),\qquad 0\le\theta(t)\leqslant 1
$$
и, следовательно, на ${\cal M}_{\varepsilon +}$ имеем:
$$
\delta_u F = F_u^\circ \delta u + (F_u^\theta - F_u^\theta)\delta u.
$$
Поскольку на ${\cal M}_{\varepsilon +}$ выполнено условие
$$
 |\delta u| = \sqrt{\varepsilon} |\Delta u|  \Rightarrow \sup_{\cal M_{\varepsilon +}} |F_u^\theta - F_u^\circ| \to 0 (\varepsilon \to 0)
$$
$$
\textrm{mes} {\cal M}_{\varepsilon +}  = \sqrt{\varepsilon},
$$
то
$$
|\int\limits_{\cal M_{\varepsilon +}} ( F_u^\theta - F_u^\circ)\,
dt| \leqslant \sup_{\cal M_{\varepsilon +}} | F_u^\theta -
F_u^\circ|(\sqrt{\varepsilon}|\Delta u|)\sqrt{\varepsilon} =
o(\varepsilon).
$$
Следовательно, имеет место оценка (\ref{3m25}). Наконец,
\begin{equation}\label{3m26}
\int\limits_{\cal M_{\varepsilon +}} F_u^\circ \delta u\, dt =
\int\limits_{\cal M_{\varepsilon +}} F_u[\tau]\delta u\, dt  +
\int\limits_{\cal M_{\varepsilon +}} (F_u^\circ -
F_u[\tau])\delta u\, dt = - F_u[\tau] \Delta u \sqrt{\varepsilon}
\sqrt{\varepsilon} + o(\varepsilon).
\end{equation}
Действительно,
$$
\sup_{\cal M_{\varepsilon +}} |F_u^\circ - F_u[\tau]| \to 0
\mbox{ при } \varepsilon \to 0,
$$
т.к. $w^\circ(t)$  непрерывна в точке $\tau$, следовательно,
$$
|\int\limits_{\cal M_{\varepsilon +}} (F_u^\circ -
F_u[\tau])\delta u\, dt| \leqslant{\cal M_{\varepsilon +}}
|F_u^\circ - F_u[\tau]| (|\Delta
u|\sqrt{\varepsilon})\sqrt{\varepsilon} = o(\varepsilon).
$$

Из формул (\ref{2m22}) - (\ref{3m26}) получаем:
$$
J(x^\circ + \delta x) -J(x_0) = \Delta F \cdot \varepsilon -
F_u[\tau]\Delta u \cdot \varepsilon + o(\varepsilon).
$$
Остается разделить на $\varepsilon$  и перейти к пределу. Тогда
получаем
\begin{equation}\label{3m27}
\Delta F - F_u[\tau]\Delta u \geqslant 0.
\end{equation}

Пусть теперь  $t_* \in (t_0, t_1)$ - точка разрыва функции
$u^\circ(t)$. Тогда су\-щест\-вуют и конечны  $u^\circ(t_* - 0)$ и
$u^\circ(t_* + 0)$. Переходя в условии (\ref{3m27}) к пределу при
$\tau \to t_* - 0$, получаем
$$
F(t_*, x^\circ(t_*), u^\circ(t_* - 0) + \Delta u) - F(t_*, x^\circ(t_*), u^\circ(t_* - 0)) - F_u[\tau]\Delta u \ge 0,
$$
где
$$
F_u[\tau] = F_u(t_*, x^\circ(t_*),\quad u^\circ(t_* - 0)) =
F_u(t,x^\circ(t_*),\quad u^\circ(t_* + 0))
$$
(поскольку $\psi(t) = F_u(t,w^\circ(t))$   не рвется!).

Аналогичное условие имеет место справа от $t_*$:
$$
F(t_*, x^\circ(t_*), u^\circ(t_* + 0) + \Delta u) - F(t_*, x^\circ(t_*), u^\circ(t_* + 0)) - F_u[\tau]\Delta u \ge 0.
$$

Итак, доказана

\begin{theorem}.
Пусть  $x^\circ(\cdot)$ доставляет сильный минимум в простейшей задаче в классе  $KC^1$. Тогда для любой точки $t$ непрерывности  функции $\dot x^\circ(t)$ имеет место условие Вейерштрасса:

$$
{\cal E}(t,x^\circ(t),\dot x^\circ(t), \dot x^\circ(t) + \Delta
u) \geqslant 0
$$
для всех $\Delta u$  таких, что
$(t,x^\circ(t), \dot x^\circ (t) + \Delta u) \in Q$, где
$$
{\cal E}(t,x,u,v) = F(t,x,v) - F(t,x,u)- F'_u(t,x,u)(v-u).
$$
\end{theorem}

Отметим \emph{геометрический смысл} условия Вейерштрасса.
Рассмотрим функцию, зависящую только от $u$:
$$
z = f(u)
$$
Неравенство $f(u_0 + v) - f(u_0) - f'(u_0) v\geqslant 0$
(фигурирующее в условии Вейерштрасса) означает, что график
функции $z = f(u)$ лежит над ка\-са\-тель\-ной к графику  в точке
$u_0$


%\begin{figure}[htb]
%\Figure{l3_1}{10.80}{7.00}{Рис.~3.1}
%\end{figure}
%\картинка!!!!!!!!!
\textbf{Смотри рисунок $L3-1.pcx$}


(точнее, не ниже касательной). Если функция выпукла, то это
условие автоматически выполнено. Поэтому условие Вейерштрасса
всегда выполнено, если \emph{интегрант} $F(t,x,u)$ яв\-ляет\-ся
выпуклой функцией по  $u$. Как уже отмечалось, траекторию $x(t)$,
удов\-лет\-во\-ряю\-щую уравнению Эйлера
$$
\frac{d}{dt} F_{\dot x}(t,x,\dot x) = F_x(t,x,\dot x),
$$
называют \emph{экстремалью}.

Однако  весьма часто понятие экстремали включает требование,
чтобы для $x(t)$ было также выполнено условие Вейерштрасса. Чтобы
различать эти два понятия, мы договоримся: траекторию,
удов\-лет\-во\-ряю\-щую только уравнению Эйлера, называть
\emph{слабой} экстремалью, а траекторию, удов\-лет\-во\-ряю\-щую
уравнению Эйлера и условию Вейерштрасса, называть \emph{сильной}
экстремалью. В оптимальном управлении (где условие
Вейершт\-рас\-са имеет своим аналогом принцип максимума
Понтрягина) пос\-лед\-нюю называют еще понтрягинской экстремалью
(или просто экст\-ре\-ма\-лью).

\subparagraph{9. Канонические переменные. Принцип максимума. Функция Понтрягина.}

Итак, <<слабая>> экстремаль в простейшей задаче
оп\-ре\-де\-ляе\-мая уравнением Эйлера:
\setcounter{equation}{0}
\begin{equation}\label{3m1}
\frac{d}{dt} F_{\dot x}(t,x,\dot x) = F_x(t,x,\dot x).
\end{equation}
Положим
\begin{equation}\label{3m2}
\dot x = u,\qquad \psi = F_u(t,x,u).
\end{equation}
Тогда уравнение Эйлера оказывается эквивалентно системе
\begin{equation}\label{3m3}
  \left\{
   \begin{array}{rcl}
    \dot   x &   =& u          \\
    \dot  \psi&  =& F_x(t,x,u)  \\
    \psi      &  =& F_u(t,x,u) \\
   \end{array}
   \right.
\end{equation}

Положим
$$
H=\psi u - F(t,x,u).
$$
Таким образом, $H=H(t,x,u,\psi)$. Тогда система (\ref{3m3}) может
быть пе\-ре\-пи\-са\-на в виде
\begin{equation}\label{3m4}
  \left\{
   \begin{array}{rcl}
    \dot   x    = H_\psi (t,x,u,\psi)          \\
    - \dot \psi = H_x(t,x,u,\psi)  \\
     H_u(t,x,u,\psi) = 0 \\
    \end{array}
   \right.
\end{equation}

Переменная $\psi$  называется \emph{двойственной} или
\emph{сопряженной}, а пе\-ре\-мен\-ная $u$~--- \emph{управлением}.
(Обозначение $\psi$ введено Л.С.Понтрягиным и его сотрудниками
для задач оптимального управления. Другое, часто
встре\-чаю\-щее\-ся обозначение для сопряженной переменной - это
$p$,   - связано с тем, что в задачах механики сопряженная
переменная есть импульс системы).

Предположим, что выполнены условия (например, $F_{uu}\ne 0$  на $Q$ , или, более того, $F_{uu} > 0$  на $Q$; последнее гарантирует строгую выпуклость $F$  по переменной $u$), позволяющие использовать условие
$$
H_u(t,x,u,\psi)=0,
$$
или, что тот же самое, условие
$$
\psi =F_u(t,x,u),
$$
для того, чтобы выразить управление $u$  как функцию от
$t,x,\psi$:
\begin{equation}\label{3m5}
u=U(t,x,\psi).
\end{equation}
Пусть
$$
H_u(t,x,U(t,x,\psi),\psi) \equiv  0  \ \forall t,x,\psi.
$$
Рассмотрим функцию
\begin{equation}\label{3m6}
{\cal H}(t,x,\psi) = H(t,x,U(t,x,\psi),\psi).
\end{equation}
Имеем:
$$
\frac{\partial{\cal H}}{\partial\psi} =\left(\frac{\partial
H}{\partial u}\frac{\partial U}{\partial\psi} + \frac{\partial
H}{\partial\psi}\right) |_{u=U(t,x,\psi)}
$$
Но $U(t,x,\psi)$  найдена из условия $H_u=0$. Следовательно,
$$
\frac{\partial{\cal H}}{\partial\psi} = \frac{\partial
H}{\partial\psi}
$$
Аналогично,
$$
\frac{\partial{\cal H}}{\partial x} = \frac{\partial H}{\partial
x}.
$$

Итак, мы получаем: если $x(\cdot)$~--- экстремаль, а
$\psi(\cdot)$~--- соот\-ветст\-вую\-щая двойственная переменная
$(\psi(t)=F_u(t,x(t),\dot x(t)))$, то пара функций  $x(t),
\psi(t)$ удовлетворяет системе уравнений
\begin{equation}\label{3m7}
  \left\{
   \begin{array}{rcl}
    \dot   x    = {\cal H}_\psi (t,x,\psi)          \\
    - \dot \psi = {\cal H}_x(t,x,\psi)
    \end{array}
   \right.
\end{equation}

Системы такого вида называют \emph{гамильтоновыми}, а переменные
$x,\psi$~--- \emph{каноническими}.

Отметим, что система (\ref{3m4}) гамильтоновой не является,
поскольку со\-дер\-жит параметр  $u$, связанный с остальными
переменными через послед\-нее соотношение $H_u = 0$. Тем не
менее, основной для нас все-таки будет система  (\ref{3m4}),
поскольку переход к гамильтоновой системе связан с
до\-пол\-ни\-тель\-ны\-ми требованиями (которыми мы не хотим себя
заранее об\-ре\-ме\-нять).

Указанный прием перехода к гамильтоновой системе и к каноническим
переменным на уровне слабых экстремалей (определяемых уравнением
Эйлера) имеет прямое отношение к так называемому классическому
\emph{преоб\-ра\-зо\-ва\-нию Лежандра}. Пусть $f=f(u)$~---
гладкая функция $(u\in\mathbb{R}^n)$. Ее классическим
преобразованием Лежандра называется новая функция $h=h(\psi)$,
определяемая равенством $h=\psi u - f(u)$, где $u=U(\psi)$
найдено из условия $\psi=f'(u)$. При этом предполагается, что
условие  $\psi=f'(u)$ позволяет однозначно найти $u$ как функцию
от $\psi : u=U(\psi)$ (например $f''(u)>0\ \forall u$ ). Ясно,
что переход от $F(t,x,u)$ к  ${\cal H}(t,x,\psi)$ есть
преоб\-ра\-зо\-ва\-ние Лежандра (классическое) функции $F$ по
аргументу $u$ (при фиксированных $t,x$, рассматриваемых в
преобразовании Лежандра в ка\-чест\-ве параметров). Подробнее на
этом не останавливаемся (см. АТФ, стр. 226).

Обратимся теперь к условию Вейерштрасса. Функция Понтрягина $H$
позволяет сформулировать условие Вейерштрасса в следующем
эк\-ви\-ва\-лент\-ном виде (проверьте эквивалентность): для любой
точки $t$ неп\-ре\-рыв\-нос\-ти <<управления>> $u^\circ(t)$ имеет
место неравенство
\begin{equation}\label{3m8}
H(t,x^\circ(t),u,\psi(t))\leqslant
H(t,x^\circ(t),u^\circ(t),\psi(t))
\end{equation}
для всех значений $u$ таких, что  $(t,x^\circ(t),u) \in Q$, \ где
\begin{equation}\label{3m9}
  \begin{array}{l}
    \psi (t)   = F_u(t,x^\circ(t),u^\circ(t))          \\
     u^\circ(t) = \dot x^\circ(t).
   \end{array}
\end{equation}
Условие (\ref{3m8}) носит название \emph{принципа максимума}. Оно
утверждает, что максимум функции Понтрягина  $H$ по переменной
$u$  достигается (при каждом $t$) на <<оптимальном управлении>>
$u^\circ(t)$. Это условие также может быть использовано для
перехода к гамильтоновой системе. А именно, полагают
\begin{equation}\label{3m10}
{\cal H}(t,x,\psi) = \sup_u H(t,x,u,\psi),
\end{equation}
где supremum  берется по $u$  таким, что $(t,x,u)\in Q$. Эту
функцию на\-зы\-вают \emph{гамильтонианом}. (Мы используем для нее
то же обозначение, что и ранее использовали для функции
(\ref{3m6}). Докажите, что если $F_{uu}>0$ всюду, то обе функции
совпадают; воспользуйтесь при этом тем обстоятельством, что для
выпуклой функции условие стационарности не только необходимо, но и
достаточно для минимума. В общем случае функции (\ref{3m6}) и
(\ref{3m10}) могут не совпадать). При определенных условиях
доказывается, что сильные экстремали (т.е. удовлетворяющие
уравнению Эйлера и условию Вейерш\-трас\-са) могут быть найдены
как решения гамильтоновой системы с га\-миль\-то\-ниа\-ном
(\ref{3m10}). Переход к этому гамильтониану связан с
преоб\-ра\-зо\-ва\-нием, известном в выпуклом анализе под
названием преобразования Юнга - Фенхеля. Дадим определение.

Пусть $f=f(u)$~--- некоторая функция. Ее преобразованием
Юнга-Фенхеля называется новая функция $h=h(\psi)$, определенная
равенством:
$$
h(\psi) = \sup_u \{ \psi u - f(u)\}
$$
Полагают $h(\psi) = f^*(\psi)$  и называют  $f^*$ функцией,
сопряженной к $f$. Сопряженная функция является выпуклой. Ясно,
что гамильтониан  $\cal H$, определенный равенством (\ref{3m10}),
есть преобразование Юнга - Фенхеля по переменной $u$ (при
фиксированных $t$ и $x$) от функции П онтрягина $H$. Подробнее об
этом преобразовании см. АТФ, с.224.

В дальнейшем мы получим принцип максимума для класса задач,
охватывающего не только все вариационное исчисление, но и
многие задачи оптимального управления.


\endinput
