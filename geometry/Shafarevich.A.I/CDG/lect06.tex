\setcounter{deff}{0}
\setcounter{utv}{0}
\setcounter{pred}{0}
\setcounter{prim}{0}
\setcounter{lemma}{0}
\setcounter{zam}{0}
\setcounter{zad}{0}
\setcounter{theorem}{0}
\setcounter{sled}{0}
\setcounter{equation}{0}
\newpage
\parindent=1cm
\section{Лекция 6. Деривационные формулы. Восстановление поверхности по паре
квадратичных форм.  Уравнения Гаусса и Кодацци. Теорема
Гаусса. Гауссова кривизна}
\paragraph{$\mathbf{\S\, 1.}$ Деривационные формулы.\\}
\hspace{0.6cm}С каждой точкой гиперповерхности связан базис $r_1,\dots, r_n, m$
объемлющего пространства; этот базис можно считать аналогом репера Френе,
возникающего в теории кривых (хотя, в отличие от репера Френе, базис,
порожденный гиперповерхностью, не ортонормирован). Естественно поэтому
попытаться получить аналог формул Френе --- равенства, выражающие
производные от базисных векторов $r_1,\dots, r_n, m$\, через сами эти
векторы и, тем самым, описывающие движение базиса при изменении точки
поверхности. Такие формулы в теории поверхностей называются деривационными
формулами.
\begin{theorem}[Деривационные формулы Гаусса--Вейнгартена]
Обозначим через $r_{ij}$ и $m_j$ производные векторов $r_i,m$ по
координате $u^j$:
$$
r_{ij}=\frac{\partial^2 r}{\partial u^i\partial u^j}\,,\quad m_j=\frac{\partial m}{\partial u^j}.
$$
Имеют место равенства:
\begin{align}
&r_{ij}=\sum_{k=1}^n\Gamma^k_{ij}r_k+b_{ij}m,\nonumber\\
\smallskip\\
&m_i=\sum_{j\,=1}^n\beta_i^jr_j\nonumber.
\end{align}
Здесь $\Gamma^k_{ij}$ --- символы Кристоффеля (формулы для них получены в
Лекции 4), $b_{ij}$ --- коэффициенты второй квадратичной формы поверхности
(см. предыдущую Лекцию), коэффициенты $\beta_i^j$ выражаются через первую и
вторую квадратичные формы следующим образом:
$$
\beta_i^j=-\sum_{k=1}^ng^{jk}b_{ik}
$$
(напомним, что матрица $g^{ij}$ обратна к матрице $g_{ij}$).
\end{theorem}
\begin{proof}
Разложим вектор $r_{ij}$ на касательную и нормальную к поверхности $M$
компоненты:
$$
r_{ij}=\Pi(r_{ij})+m_{ij},
$$
где $\Pi$ --- оператор ортогонального проектирования на касательную
плоскость к поверхности, $m_{ij}$ --- проекция вектора $r_{ij}$ на
нормаль. Вектор $\Pi(r_{ij})$ вычислен в Лекции 4 (формулы (2) -- (3)):
$$
\Pi(r_{ij})=\sum_{k=1}^n\Gamma^k_{ij}r_k;
$$
вектор $m_{ij}$, очевидно, равен:
$$
m_{ij}=(m,r_{ij})m=b_{ij}m.
$$
Таким образом, первая часть формул (1) доказана. Для вычисления векторов
$m_j$ заметим, что они ортогональны вектору $m$ (т.к. последний имеет
единичную длину --- см. Лемму, доказанную в первой Лекции), и поэтому
разлагаются по каноническому базису $r_1,\dots, r_n$:
\begin{align}
m_i=\sum_{k=1}^n\beta_i^kr_k,
\end{align}
где $\beta_i^k$ --- некоторые (пока неопределенные) коэффициенты. Для того,
чтобы их найти, умножим (2) скалярно на $r_m$; получим:
$$
(m_i,r_m)=\sum_{k=1}^n\beta^k_ig_{km},
$$
откуда:
$$
\beta^j_i=\sum_{s\,=1}^ng^{js}(m_i,r_s).
$$
Наконец, дифференцируя по $u^i$ равенство $(m,r_m)=0$, получим
$(m_i,r_m)+b_{im}=0$, откуда немедленно следует нужное выражение для
$\beta_i^k$.
\end{proof}
Как уже отмечалось, деривационные формулы похожи на формулы Френе теории
кривых --- они описывают движение репера $r_1,\dots, r_n, m$ при движении
точки по поверхности. Это движение, таким образом, определяется парой
квадратичных форм --- первой и второй формами поверхности, которые в этом
смысле играют ту же роль, что кривизна и кручение кривой. Напомним, что
задание кривизны и кручения полностью определяет кривую с точностью до ее
расположения в пространстве (т.е. с точностью до его движения); кроме
того, эту пару функций можно задавать произвольно: если  $k(s),\varkappa(s)$ ---
гладкие функции и $k>0$, всегда найдется кривая, для которой эти функции
будут кривизной и кручением. Доказательство этих фактов основано на
анализе дифференциальных уравнений, определенных формулами Френе.

Выясним, как обстоит дело в случае поверхностей; таким образом, нас сейчас
интересуют два вопроса:
\begin{enumerate}
\item Определяется ли поверхность (с точностью до своего расположения в
пространстве) заданием первой и второй квадратичных форм?
\item Можно ли эту пару форм задавать произвольно (позаботившись лишь о
положительности первой квадратичной формы)?
\end{enumerate}
Оказывается, ответы на эти два вопроса противоположны: первый решается
положительно, а второй отрицательно. Мы начнем с обсуждения первого.
\paragraph{$\mathbf{\S\, 2.}$ Восстановление поверхности по паре квадратичных форм.}
\hspace{0.6cm}
\begin{theorem}
Пусть $M$, $Q$ --- две $n$--мерные гиперповерхности. Следующие два условия
эквивалентны.
\begin{enumerate}
\item На поверхностях $M$ и $Q$ можно выбрать единые координаты
$u=(u^1,\dots, u^n)$\linebreak (а также направления единичных векторов нормалей)
так, что матрицы первой и второй квадратичных форм в этих координатах
совпадут.
\item Существует движение (т.е. композиция сдвига и ортогонального
преобразования) пространства $\Bbb R^{n+1}$, переводящее поверхность $M$
в поверхность $Q$.
\end{enumerate}
\end{theorem}
\begin{proof}
Пусть $x\mapsto Ax+a$ --- движение  объемлющего пространства, переводящее
поверхность $M$ в поверхность $Q$ (здесь $A$ --- ортогональный оператор
в $\Bbb R^{n+1}$, $a$~--- вектор из этого пространства). Зададим $M$
(какими угодно) параметрическими уравнениями \mbox{$r=r(u)$}; тогда точки
поверхности $Q$ можно задать уравнениями $r=\rho(u)$, где
$\rho(u)=Ar(u)+a$. В силу ортогональности оператора $A$:
$$
(\frac{\partial r(u)}{\partial u^i},\frac{\partial r(u)}{\partial u^j})=
(\frac{\partial \rho(u)}{\partial u^i},\frac{\partial \rho(u)}{\partial u^j}),
$$
т.е. матрицы первых квадратичных форм поверхностей $M$ и $Q$ в координатах
$u$ совпадают. Заметим теперь, что, если $m(u)$ --- единичный вектор
нормали к поверхности $M$, то $Am(u)$~--- единичный вектор
нормали к поверхности $Q$; поэтому из равенств:
$$
(\frac{\partial ^2 r(u)}{\partial u^i\partial u^j},m)=
(\frac{\partial ^2 \rho(u)}{\partial u^i\partial u^j},Am)
$$
следует, что матрицы вторых квадратичных форм также совпадают. Итак, из
второго условия следует первое.

Обратно, пусть на поверхностях $M$, $Q$ выбраны координаты так, что
матрицы первой и второй квадратичных форм в этих координатах одинаковы.
Тогда между точками этих поверхностей можно установить
взаимно--однозначное соответствие: именно, соответствующими друг другу
назовем точки, имеющие одинаковые координаты. Выберем теперь на
поверхностях $M$ и $Q$ пару соответствующих точек и сдвинем поверхность
$Q$ на вектор, соединяющий эти точки; в результате новая поверхность будет
иметь общую точку с поверхностью $M$. Рассмотрим теперь канонические
базисы, порожденные координатами $u^i$ в касательных пространствах к двум
поверхностям в этой общей точке. В силу совпадения матриц первых
квадратичных форм, длины соответствующих векторов в этих базисах и углы
между соответствующими векторами одинаковы. Поэтому оператор, переводящий
канонический базис и единичный вектор нормали второй поверхности в
канонический базис и единичный вектор нормали первой поверхности в их
общей точке --- ортогональный (сохраняет длины и углы). Итак, композицией
сдвига и ортогонального оператора можно добиться того, что наши две
поверхности будут иметь общую точку (обозначим ее через~$P_0$) и в этой
точки совпадут соответствующие вектора канонических базисов касательных
пространств и единичных нормалей (обозначим эти векторы $r_1^0,\dots,
r_n^0,m^0$~соответственно).  Докажем теперь, что такое движение
объемлющего пространства совмещает поверхности, т.е.  что соответствующие
точки поверхности $M$ и поверхности $\widetilde M$, полученной из $Q$ указанным
движением, совпадают.  Действительно, пусть $P$ --- произвольная точка
$M$; соединим ее с точкой $P_0$ гладкой кривой $\gamma\subset M$, заданной
параметрическими уравнениями $u^i=u^i(t),\,t\in [0,T]$. Кривая
$\widetilde\gamma$, заданная теми же параметрическими уравнениями на $\widetilde M$,
соединяет $P_0$ с точкой $\widetilde P$, соответствующей точке $P$. Рассмотрим
теперь семейства векторов $r_i(t),m(t)$, состоящих из векторов
канонического базиса и нормали к поверхности $M$ в точках кривой $\gamma$.
Производные этих векторов по параметру $t$ имеют вид:
$$
\frac{dr_i(t)}{dt}=\frac{\partial r_i}{\partial
u^j}\frac{du^j}{dt}=\Gamma_{ij}^k(u(t))\frac{du^j}{dt}r_k(t)+b_{ij}(u(t))
\frac{du^j}{dt}m(t),
$$
$$
\frac{dm(t)}{dt}=\frac{\partial m}{\partial
u^j}\frac{du^j}{dt}=\beta_j^k(u(t))
\frac{du^j}{dt}r_k(t).
$$
Таким образом, если обозначить через $R(t)$ матрицу размера $(n+1)\times
(n+1)$,
строками которой являются координаты векторов
$r_1(t),\dots,r_n(t),m(t)$, эта матрица будет удовлетворять уравнению:
$$
\frac{dR(t)}{dt}=U(t)R(t),
$$
где элементы матрицы $U(t)$ выражаются через элементы матриц первой и
второй квадратичной форм поверхности $M$ (а также их производные) при
$u=u(t)$ и через производные функций $u^i(t)$. Рассмотрим теперь матрицу
$\widetilde R(t)$, построенную аналогичным образом по векторам $\widetilde r_1,\dots,\widetilde
r_n,\widetilde m$ канонического базиса и нормали поверхности $\widetilde M$ в точках
кривой~$\widetilde\gamma$. Поскольку эти две кривые заданы в координатах $u^i$
одинаковыми параметрическими уравнениями, а матрицы первой и второй
квадратичных форм поверхностей $M,\widetilde M$ совпадают, для $\widetilde R$ будет
выполнено то же уравнение (с той же матрицей $U(t)$), что и для $R$.
Далее, при $t=0$ (т.е. в точке $P_0$) матрицы $R(t)$ и $\widetilde R(t)$
совпадают, т.к. совпадают соответствующие векторы. Следовательно, по
теореме единственности решения задачи Коши для линейной системы
обыкновенных дифференциальных уравнений, матрицы $R$ и $\widetilde R$ совпадают
при всех $t\in [0,T]$, т.е. при таких $t$: $r_i(t)=\widetilde r_i(t),m(t)=\widetilde
m(t)$. Рассмотрим теперь радиус--векторы~$r(t)$ точек кривой
$\gamma$ на поверхности $M$.  По формуле Ньютона--Лейбница получаем:
$$
r(P)=r(T)=r(0)+\int_0^T(\frac{dr}{dt})=r(P_0)+\int_0^Tr_i(t)\frac{du^i}{dt}\,dt.
$$
Аналогично, рассматривая радиус--векторы $\widetilde r(t)$ точек кривой $\widetilde\gamma$,
получим:
$$
r(\widetilde P)=\widetilde
r(T)=\widetilde r(0)+\int_0^T(\frac{d\widetilde r}{dt})=r(P_0)+\int_0^T\widetilde
r_i(t)\frac{du^i}{dt}\,dt.
$$
Поскольку подинтегральные функции в двух выписанных интегралах совпадают,
совпадают и радиус--векторы точек $P$ и $\widetilde P$, т.е. совпадают
поверхности $M$ и $\widetilde M$.
\end{proof}
\paragraph{$\mathbf{\S\, 3.}$ Формулы Гаусса и Кодацци.\\}
\hspace{0.6cm}Исследуем теперь вопрос о том, насколько произвольно можно задавать
матрицы первой и второй квадратичной форм поверхности. Другими словами,
пусть заданы две гладкие симметричные матричные функции $g_{ij}(u)$ и
$b_{ij}(u)$, причем матрица $g_{ij}(u)$ положительно определена. Можно ли
найти поверхность, заданную параметрическими уравнениями $r=r(u)$ так,
чтобы эти заданные функции совпали с ее первой и второй квадратичными
формами? Ответ на этот вопрос отрицательный; мы исследуем препятствие к
построению такой поверхности в простейшем случае $n=2$ (т.е. двумерной
поверхности в трехмерном пространстве).
\begin{theorem}
Пусть $M$ --- двумерная поверхность в $\Bbb R^3$, заданная
параметрическими уравнениями $r=r(u^1,u^2)$, и пусть $g_{ij}(u),b_{ij}(u)$
--- матрицы ее первой и второй квадратичной форм соответственно. Имеют
место три соотношения между элементами этих матриц:
$$
\operatorname{det}B=\frac{\partial ^2g_{12}}{\partial u^1\partial u^2}
-\frac12\frac{\partial ^2g_{11}}{(\partial u^2)^2}-\frac12\frac{\partial ^2g_{22}}{(\partial
u^1)^2}+\sum_{k,\,m=1}^2g_{km}(\Gamma^k_{12}\Gamma^m_{12}-\Gamma^k_{11}\Gamma^m_{22});
$$
$$
\frac{\partial b_{i1}}{\partial u^2}+\sum_{k=1}^2\Gamma^k_{i1}b_{k2}=
\frac{\partial b_{i2}}{\partial u^1}+\sum_{k=1}^2\Gamma^k_{i2}b_{k1},\quad i=1,2.
$$
Здесь $B$ --- матрица второй квадратичной формы.
\end{theorem}
\begin{zam}
Первое из этих соотношений называется {\it уравнением Гаусса}, а два
последних --- {\it уравнениями Кодацци}.
\end{zam}
\begin{proof}
Докажем сперва равенство Гаусса--Вейнгартена. Для этого рассмотрим
скалярную функцию:
$$
(r_{11},r_{22})-(r_{12}^2)
$$
и подставим выражения для $r_{ij}$ из деривационных формул Гаусса.
Учитывая ортогональность векторов $m$ и $r_i$, получим:
$$
(r_{11},r_{22})-(r_{12}^2)=
\sum_{k,\,m=1}^2(r_k,r_m)(\Gamma^k_{11}\Gamma^m_{22}-\Gamma^k_{12}\Gamma^m_{12})+(b_{11}b_{22}-b_{12}^2),
$$
откуда:
$$
\operatorname{det}B=(r_{11},r_{22})-(r_{12}^2)+
\sum_{k,\,m=1}^2g_{km}(\Gamma^k_{12}\Gamma^m_{12}-\Gamma^k_{11}\Gamma^m_{22}).
$$
Таким образом, для доказательства равенства Гаусса достаточно проверить,
что:
$$
(r_{11},r_{22})-(r_{12}^2)=\frac{\partial ^2g_{12}}{\partial u^1\partial u^2}
-\frac12\frac{\partial ^2g_{11}}{(\partial u^2)^2}-\frac12\frac{\partial ^2g_{22}}{(\partial
u^1)^2}.
$$
Для этого вспомним, что символы Кристоффеля 1-го рода:
$$
\Gamma_{ij,k}=(r_{ij},r_k)
$$
выражаются через коэффициенты первой квадратичной формы по формулам:
$$
\Gamma_{ij,k}=\frac12(\frac{\partial g_{ik}}{\partial u^j}+\frac{\partial g_{jk}}{\partial
u^i}-\frac{\partial g_{ij}}{\partial u^k}).
$$
Продифференцируем это равенство по координате $u^l$; в результате получим:
$$
\frac{\partial\Gamma_{ij,k}}{\partial u^l}=(r_{lij},r_k)+(r_{ij},r_{lk})=
\frac12(\frac{\partial^2 g_{ik}}{\partial u^l\partial u^j}+\frac{\partial^2 g_{jk}}{\partial u^l\partial
u^i}-\frac{\partial^2 g_{ij}}{\partial u^l\partial u^k}),
$$
где:
$$
r_{lij}=\frac{\partial^3r}{\partial u^l\partial u^i\partial u^j}.
$$
Эти равенства справедливы при всех значениях индексов $i,j,k,l$, поэтому в
них можно поменять местами $l$ и $j$. Вычитая получающееся при этом
равенство из выписанного, и учитывая, что $r_{ij\,l}=r_{i\,lj}$ (теорема о
равенстве смешанных производных), получим:
$$
(r_{ij},r_{lk})-(r_{il},r_{jk})=
\frac12(\frac{\partial^2 g_{il}}{\partial u^j\partial u^k}+\frac{\partial^2 g_{jk}}{\partial u^l\partial u^i}-\frac{\partial^2 g_{lk}}{\partial u^i\partial
u^j}+\frac{\partial^2 g_{jk}}{\partial u^l\partial u^i}-\frac{\partial^2 g_{ij}}{\partial u^l\partial
u^k}).
$$
Полагая в последней формуле $i=j=1$, $l=k=2$, получим требуемое равенство.

Докажем теперь формулы Кодацци. Для этого запишем коэффициенты второй
квадратичной формы в виде:
$$
b_{ij}=(r_{ij},m)=-(r_i,m_j),
$$
где $m_j=\partial m/\partial u^j$. Продифференцируем это равенство при $j=2$ по
переменной $u^1$, а при $j=1$ по переменной $u^2$, и вычтем друг из друга
два получившихся соотношения. С учетом равенства смешанных производных
вектора $m$ получим:
$$
\frac{\partial b_{i2}}{\partial u^1}-\frac{\partial b_{i1}}{\partial
u^2}+(r_{i1},m_2)-(r_{i2},m_1)=0.
$$
Подставим в полученное соотношение выражение для $r_{ij}$ из деривационных
формул \mbox{Гаусса}. Учитывая, что $(m_k,m)=0$ и $(m_k,r_s)=-b_{ks}$, получим:
$$
\frac{\partial b_{i2}}{\partial u^1}-\frac{\partial b_{i1}}{\partial
u^2}-\sum_{k=1}^2\Gamma_{i1}^kb_{k2}+\sum_{k=1}^2\Gamma_{i2}^kb_{k1}=0,
$$
что и требовалось.
\end{proof}
\begin{sled}
Отнюдь не любые симметричные матричные функции $g_{ij}(u)$ и $b_{ij}(u)$
(даже при условии положительности первой матрицы) могут быть реализованы
как первая и вторая квадратичные формы некоторой поверхности --- для этого
они, по крайней мере, должны удовлетворять равенствам Гаусса и Кодацци.\quad$\blacksquare$
\end{sled}
\begin{zam}
Можно показать, что равенства Гаусса и Кодацци не только необходимы, но и
достаточны для того, чтобы существовала поверхность, для которой $g_{ij}$
и $b_{ij}$ были бы первой и второй квадратичной формами. Именно,
справедливо следующее утверждение.\quad$\blacksquare$
\end{zam}
\begin{theorem}
Пусть в некоторой открытой области на плоскости ($u^1,u^2$) задана пара
гладких матричных функций $g_{ij}(u)$, $b_{ij}(u)$, удовлетворяющих
следующим условиям:
\begin{enumerate}
\item При каждом $u^i$ обе матрицы симметричны, а матрица $g_{ij}(u)$
положительно определена.
\item Функции $g_{ij}$ и $b_{ij}$ удовлетворяют уравнениям Гаусса и Кодацци.
\end{enumerate}
Тогда в достаточно малой окрестности произвольной точки этой
области найдется трехмерная вектор--функция $r=r(u)$, задающая двумерную
поверхность в $\Bbb R^3$, для которой заданные матрицы являются матрицами
первой и второй квадратичных форм соответственно (по доказанному выше эта
поверхность определена однозначно с точностью до движения пространства).

Доказательство этой теоремы выходит за рамки нашего курса. Отметим, что
деривационные формулы Гаусса--Вейнгартена можно трактовать как систему
уравнений в частных производных на координаты вектор--функции $r(u)$,
коэффициенты которой заданы, если заданы матрицы $g_{ij}$ и $b_{ij}$.
Уравнения Гаусса и Кодацци суть условия совместности (разрешимости) этой
системы.\quad$\blacksquare$
\end{theorem}
\paragraph{$\mathbf{\S\, 4.}$ Гауссова кривизна двумерной поверхности. Теорема Гаусса. Эллиптические,
гиперболические и параболические точки.\\}
\hspace{0.6cm}Отметим, что из доказанной в предыдущем пункте формулы Гаусса
немедленно вытекает следующее
\begin{utv}[Теорема Гаусса]
Гауссова кривизна двумерной поверхности в $\Bbb R^3$ выражается только
через коэффициенты первой квадратичной формы.\quad$\blacksquare$
\end{utv}
Действительно, гауссова кривизна определяется через матрицы $G$ и $B$
первой и второй квадратичной форм по формуле:
$$
K=\frac{\operatorname{det}B}{\operatorname{det G}}.
$$
С другой стороны, уравнение Гаусса выражает определитель второй
квадратичной формы через первую.

Это означает, что {\it гауссова кривизна зависит только от геометрии на
самой поверхности (а не от того, как поверхность помещена в трехмерное
пространство); в частности, она сохраняется при изометриях ---
отображениях поверхностей, не меняющих скалярного произведения
касательных векторов (подробнее об изометриях см.~следующую Лекцию)}.

Обсудим геометрический смысл знака гауссовой кривизны двумерной
поверхности.  Поскольку он совпадает со знаком определителя
второй квадратичной формы, этот знак  определяет локальное
расположение поверхности $M$ относительно касательной плоскости к ней в
точке $P$ (см. предыдущую Лекцию).  Именно, пусть в некоторой точке
поверхности $K>0$; тогда главные кривизны в этой точке одного знака,
т.е. все нормальные сечения искривлены в одну и ту же сторону ($\widetilde k>0$
или $\widetilde k<0$ сразу для всех нормальных сечений).  Другими словами, для
всех таких сечений центры соприкасающихся окружностей лежат по одну и ту
же сторону от касательной плоскости к поверхности.  Поэтому достаточно
малая окрестность точки $P$ на поверхности лежит по одну сторону от
касательной плоскости и не пересекает ее нигде, кроме точки $P$.
\begin{deff}
Точки, в которых $K>0$, называются эллиптическими точками поверхности $M$.
\end{deff}
Пусть теперь $K(P)<0$. Тогда главные кривизны разного знака; нормальные
сечения, проведенные в главных направлениях, загнуты в разные стороны.
Поэтому сколь угодно малые кусочки этих нормальных сечений вблизи точки $P$
лежат по разные стороны от касательной плоскости к $M$ в этой точке.
Таким образом, любая малая окрестность точки $P$ на поверхности $M$
разбивается на две части, лежащие по разные стороны от касательной
плоскости; касательная плоскость пересекает любую такую окрестность,
вообще говоря, по некоторой кривой.
\begin{deff}
Точки, в которых $K<0$, называются гиперболическими точками поверхности
$M$.
\end{deff}
Наконец, в некоторых точках поверхности гауссова кривизна может обращаться
в нуль. В этом случае первая и вторая квадратичная формы не дают полной
информации о том, как расположена малая окрестность точки $P$ на
поверхности относительно касательной плоскости; для того, чтобы ответить
на этот вопрос, необходимо рассматривать производные третьего порядка
вектора $r(u^1,u^2)$ в точке $P$.
\begin{deff}
Точки, в которых $K=0$, называются параболическими точками поверхности $M$.
\end{deff}
\newpage
\begin{center}
\section*{Задачи.}
\end{center}
\begin{enumerate}
\item Найти эллиптические, гиперболические и параболические точки:
\begin{enumerate}
\item на торе;
\item на эллипсоиде, гиперболоиде, параболоиде;
\item на гиперболическом цилиндре;
\item на поверхности $z=f(\sqrt{x^2-y^2})$.
\end{enumerate}
\end{enumerate}
