\setcounter{deff}{0}
\setcounter{utv}{0}
\setcounter{pred}{0}
\setcounter{prim}{0}
\setcounter{lemma}{0}
\setcounter{zam}{0}
\setcounter{zad}{0}
\setcounter{theorem}{0}
\setcounter{sled}{0}
\setcounter{equation}{0}
\newpage
\parindent=1cm
\section{Лекция 14. Вложения и погружения многообразий}

\paragraph{$\mathbf{\S\, 1.}$ Дифференциал гладкого отображения.\\}
\hspace{0.6cm}Выше мы видели, что гладкое отображение поверхностей индуцирует
отображение касательных векторов к ним: вектор скорости некоторой кривой
на первой поверхности переходит в вектор скорости образа этой кривой (в
соответствующей точке) на второй поверхности.  Точно так же обстоит дело и
в случае произвольных многообразий. Именно, пусть $M,Q$ --- гладкие
многообразия размерностей $n,m$ соответственно, и пусть $f: M\to Q$~---
гладкое отображение. Рассмотрим произвольный касательный вектор $\xi$ к
многообразию $M$ в точке $P$. Это --- класс касающихся в точке $P$ гладких
кривых на $M$; пусть $\gamma$ --- одна из кривых (представитель) этого класса.
Отображение $f$ переводит эту кривую в гладкую кривую $f(\gamma)$, лежащую на
многообразии $Q$ и проходящую через точку $f(P)$; обозначим
через $d_Pf(\xi)$ вектор скорости кривой $f(\gamma)$ в этой точке.
\begin{theorem}
Вектор $d_Pf(\xi)$ не зависит от выбора кривой $\gamma$ из класса,
определяющего вектор $\xi$; таким образом, описанная конструкция
корректно определяет отображение $d_Pf:T_PM\to T_{f(P)}Q$. Это отображение
--- линейный оператор; пусть на многообразиях $M,Q$ заданы локальные
координаты  $x^1,\dots, x^n$ и $y^1,\dots,y^m$ в окрестностях точек $P$ и
$f(P)$ соответственно, причем отображение $f$ в этих координатах задается
гладкими функциями $y^i=f^i(x^1,\dots,x^n)$, $i=1,\dots, m$, тогда матрица
оператора $d_Pf$ в соответствующих канонических базисах --- это матрица
Якоби $\partial f^i/\partial x^j$.
\end{theorem}
\begin{proof}
Пусть уравнения кривой $\gamma$ в координатах $x^1,\dots,x^n$ имеют вид
\mbox{$x^i=x^i(t)$}; тогда ее образ $f(\gamma)$ задается уравнениями $y^i=f^i(x(t))$.
Вектор $\xi$ в каноническом базисе $\partial/\partial x^1,\dots,\partial/\partial x^n$ имеет
координаты $\xi^i=\dot x^i$, а вектор $d_Pf(\xi)$ в каноническом базисе
$\partial/\partial y^1,\dots,\partial/\partial y^m$ задается координатами $\eta^j=\dot
f^j(x(t))$.
По теореме о дифференцировании сложной функции, получаем:
$$
\eta^j=\sum_{i=1}^n\frac{\partial f^j}{\partial x^i}\dot x^i=
\sum_{i=1}^n\frac{\partial f^j}{\partial x^i}\xi^i.
$$
Матрица Якоби $\partial f^j/\partial x^i$ не зависит от выбора кривой,
представляющей вектор $\xi$, и определяется только отображением $f$;
поэтому и вектор $d_Pf(\xi)$ также не зависит от $\gamma$. Утверждения теоремы
о линейности оператора $d_Pf$ и матрице этого оператора в канонических
базисах --- очевидные следствия приведенной формулы.
\end{proof}
\begin{deff}
Линейный оператор $d_Pf: T_PM\to T_{f(P)}Q$ называется дифференциалом
гладкого отображения $f$ в точке $P$.
\end{deff}
\begin{zam}
Вид матрицы оператора $df$ можно считать его определением; именно, назовем
дифференциалом гладкого отображения в точке $P$ линейный оператор из
$T_PM$ в $T_{f(P)}Q$, заданный в канонических базисах $\partial/\partial x^1,\dots,
\partial/\partial x^n$, $\partial/\partial y^1,\dots,\partial/\partial y^m$ матрицей Якоби $\partial f^j/\partial
x^i$ ($f^j(x)$ --- функции, задающие отображение $f$).
Доказанная выше теорема гарантирует корректность такого определения, т.е.
независимость его от выбора локальных координат $x$ и $y$ (действительно,
согласно этой теореме отображение $df$ переводит касательный вектор,
определяемый кривой $\gamma$, в вектор скорости \mbox{кривой $f(\gamma)$).\quad$\blacksquare$}
\end{zam}
\begin{zam}
Касательный вектор $\xi\in T_PM$ можно представлять себе как оператор
дифференцирования гладких функций на $M$ в точке $P$. Его образ --- это
дифференцирование гладких функций на $Q$ в точке $f(P)$, заданное формулой
$d_Pf(\xi)(g)=\xi(g\circ f)$, где $g\circ f$ --- гладкая функция на $M$,
являющаяся композицией отображения $f$ и функции $g$: $g\circ
f(u)=g(f(u))$, $u\in M$ (докажите!). Приведенную формулу также можно
считать (уже третьим) определением вектора $d_Pf(\xi)$.\quad$\blacksquare$
\end{zam}
\paragraph{$\mathbf{\S\, 2.}$ Вложения и погружения.\\}
\hspace{0.6cm}Рассматривавшиеся выше поверхности были помещены в евклидово
пространство; другими словами, каждой точке поверхности соответствовала
некоторая точка $\Bbb R^N$, причем касательное пространство к
поверхности вкладывалось в касательное пространство к $\Bbb R^N$ как
подпространство. Интересно выяснить, нельзя ли аналогичным образом
``поместить'' в евклидово пространство произвольное гладкое многообразие.
Прежде, чем исследовать этот вопрос, дадим (используя аналогию с теорией
поверхностей) формальное определение ``помещения''  одного многообразия в
другое.
\begin{deff}
Гладкое отображение $f: M\to Q$ гладкого многообразия  $M$ в гладкое
многообразие $Q$ называется погружением, если для любой точки $P\in M$
дифференциал $d_Pf$ отображения $f$ в этой точке является вложением
касательного пространства $T_PM$ в касательное пространство $T_{f(P)}Q$
(напомним, что линейный оператор называется вложением, если он имеет
нулевое ядро).
\end{deff}
\begin{deff}
Гладкое погружение $f: M\to Q$ называется вложением, если оно определяет
гомеоморфизм многообразия $M$ и его образа $f(M)$.
\end{deff}
\begin{prim}
Отображение $x(t)=\operatorname{cos}t, y(t)=\operatorname{sin}t$ --- вложение окружности,
параметризованной полярным углом $t$, в евклидову плоскость с координатами
$x,y$.
\end{prim}
\begin{prim}
Отображение $x(t)=\operatorname{cos}t, y(t)=\operatorname{sin}2t$ --- погружение окружности в
плоскость, не являющееся вложением: точки $t=\pi/2$ и $t=3\pi/2$
переходят в одну точку на плоскости.  Образ этого погружения ---
``восьмерка'' --- не гомеоморфен окружности.
\end{prim}
\paragraph{$\mathbf{\S\, 3.}$ Вложение компактного многообразия в евклидово пространство
достаточно большой размерности.\\}
\hspace{0.6cm}Перейдем теперь к построению вложений многообразий в евклидовы
пространства. Мы будем рассматривать только компактные многообразия;
отметим, что в этом частном случае погружение, взаимно--однозначно
отображающее $M$ на $f(M)$, является вложением.
Действительно, если $M$ --- компактное многообразие и $f: M\to Q$ ---
погружение, взаимно--однозначно отображающее $M$ на $f(M)$, то для всякого замкнутого
подмножества \mbox{$Y\subset M$} его образ $f(Y)$ будет замкнут в $f(M)$ (из
замкнутости $Y$ следует его компактность, откуда вытекает компактность, а
значит, и замкнутость, $f(Y)$). Это означает, что обратное отображение
$f^{-1}:  f(M)\to M$ непрерывно, т.е. $f$ задает гомеоморфизм $M$ и
$f(M)$.
\begin{theorem}
Пусть $M$ --- компактное гладкое многообразие. Для достаточно
большого $N$ существует вложение этого многообразия в евклидово
пространство $\Bbb R^N$.
\end{theorem}
\begin{proof}
Поскольку $M$ компактно, на нем существует конечный атлас $U_\alpha,\varphi_\alpha$.
Можно считать, что координатные гомеоморфизмы отображают все множества
$U_\alpha$ в единичный шар $B$ евклидова пространство $\Bbb R^n$ (докажите!).
Далее, атлас можно выбрать так, что множества $\varphi_\alpha^{-1}(B_1)$, где
$B_1\subset B$ --- шар меньшего радиуса, все еще покрывают $M$
(докажите!). Рассмотрим теперь гладкую функцию $g$ в евклидовом
пространстве $\Bbb R^n$, тождественно равную нулю вне $B$ и тождественно
равную единице внутри $B_1$. Для каждого $\alpha$ рассмотрим отображение
$f_\alpha:M\to \Bbb R^{n+1}$, определенное так:
\begin{align}
&f_\alpha(P)=0,\quad&&\text{если}\quad P\notin U_\alpha,\nonumber\\
&f_\alpha(P)=(x^1_\alpha(P)g(\varphi_\alpha(P)),\dots,
x^n_\alpha(P)g(\varphi_\alpha(P)),g(\varphi_\alpha(P))),&&\text{если}\quad P\in
U_\alpha.\nonumber
\end{align}
Здесь $x^j_\alpha(P)$ --- локальные
координаты точки $P$ в карте $U_\alpha$. Легко видеть, что $f_\alpha$
--- гладкое отображение, причем на множестве $\varphi_\alpha^{-1}(B_1)$
это отображение является вложением, т.к. задается функциями
$x^1,\dots,x^n,1$. Построим теперь по этим отображениям одно
большое отображение $f:M\to \Bbb R^{s(n+1)}$, где $s$ --- число карт в
рассматриваемом атласе. Это отображение зададим формулой:
$$
f(P)=(f_1(P),\dots,f_s(P)),
$$
где в правой части записана строка из $s$ элементов, каждый из которых сам
является ($n+1$)--мерным вектором (другими словами, пространство $\Bbb
R^{s(n+1)}$ здесь представлено в виде прямой суммы $s$ экземпляров $\Bbb R^{n+1}$).
Проверим, что $f$ --- вложение. Действительно, матрица Якоби
отображения $f$ в произвольной точке $P$ многообразия $M$ представляется в
виде ``строки'' из $s$ матриц размера $n\times (n+1)$ (матриц Якоби
отображений $f_\alpha$), причем по крайней мере одна из этих матриц имеет
ранг, равный $n$ (поскольку $f_\alpha$ является вложением на карте, содержащей
$P$). Поэтому ранг всей матрицы также равен $n$, т.е. $f$ --- погружение.
Проверим, что $f$ взаимно--однозначно отображает $M$ на его образ.
Действительно, пусть $P,Q$ --- две различные точки $M$. Точка $P$ лежит в
одной из областей вида $\varphi_\alpha^{-1}(B_1)$, поэтому в этой точке
соответствующая функция $g(\varphi_\alpha)$ равна единице. Если эта же
функция в точке $Q$ не равна единице, то образы точек $P$ и $Q$ при
отображении $f$ различны. Если же $g(\varphi_\alpha(Q))=1$, то $Q$ лежит в той же
карте $U_\alpha$, что и $P$, а значит, существует координата $x^j$, которая
принимает в этих точках разные значения. Но тогда и функция $x^jg(\varphi_\alpha)$
принимает разные значения в точках $P$ и $Q$, т.е. образы этих точек при
отображении $f_\alpha$, а значит и при отображении $f$, различны.
\end{proof}
\paragraph{$\mathbf{\S\, 4.}$ Теорема Уитни.\\}
\hspace{0.6cm}Предыдущая теорема гарантирует возможность вложения многообразия в
евклидово пространство, размерность которого зависит не только от
размерности многообразия, но и от числа карт на нем. Возникает
естественный вопрос, нельзя ли избавиться от такой зависимости,
т.е. построить вложение многообразия в пространство, размерность которого
определялась бы только размерностью самого многообразия. Этот
вопрос тесно связан с другим: нельзя ли понизить приведенную выше
оценку размерности пространства, в которое вкладывается многообразие, и
если да, то какова минимальная размерность такого пространства? Мы
приведем один из результатов в этом направлении --- теорему Уитни.
\begin{theorem}[Теорема Уитни]
Любое компактное гладкое $n$-мерное многообразие $M$ можно вложить в
евклидово пространство $\Bbb R^{2n+1}$.
\end{theorem}
\begin{proof}
Воспользуемся доказанной ранее теоремой и будем считать, что наше
многообразие уже вложено в евклидово пространство $\Bbb R^N$. Будем теперь
конструировать из этого вложения новое вложение в пространство меньшей
размерности.

Для этого мы будем просто рассматривать ортогональную проекцию этого уже
вложенного многообразия на некоторое подпространство $\Bbb R^{N-1}$ вдоль
некоторого вектора $v$. Для определенности $|\,v| = 1$. Образ многообразия
$M$ в $\Bbb R^N$ мы отождествляем с самим $M$. Мы покажем сейчас, что если
$ N > 2n + 1$, то вектор $v$ всегда можно выбрать так, что проекция вдоль
этого вектора на ортогональное ему подпространство $\Bbb R^{N-1}$:
$$
\pi :M \rightarrow \Bbb R^{N-1}
$$
будет вложением. Из этого будет следовать
утверждение теоремы, т.к. повторяя этот процесс, мы построим в конце концов
требуемое вложение.

Выясним сперва, при каких условиях проекция $\pi$ будет
погружением.  Поскольку $\pi$ с точки зрения объемлющего отображения
является линейным, то дифференциал $d\pi$ совпадает с $\pi$. Ядро
отображения $\pi$ это прямая, натянутая на вектор $v$, поэтому
дифференциал~$d_P\pi$ в фиксированной точке $P\in M$ будет вложением
касательных пространств тогда и только тогда, когда касательное
пространство $T_P M$ не содержит вектора $v$.

Рассмотрим множество $X$, элементами которого являются пары
$(P , \xi)$, где $P \in M$, $\xi$~--- единичный касательный вектор к
многообразию $M$ в точке  $P$.
\begin{zad}
Доказать, что множество $X$ является гладким ($2n-1$)-мерным многообразием.
\end{zad}

Рассмотрим отображение $f : X \rightarrow S^{N - 1}$, которое каждому
элементу $(P , \xi) \in X$ сопоставляет вектор $\xi$, рассматриваемый как
единичный вектор пространства $\Bbb R^N$ (т.е. точка ($N-1$)--мерной сферы).

Проекция $\pi$ является погружением тогда и только тогда, когда
вектор проектирования $v$ не содержится в образе отображения $f$.

Далее, найдем условия, при которых $\pi$ является взаимно-однозначным (с
образом).  Пусть $P$ и $Q$ --- две различные точки многообразия $M$. Ясно,
что они проектируются в одну и ту же точку тогда и только тогда, когда
вектор $\overrightarrow{PQ}$ коллинеарен вектору проектирования $v$. Другими словами,
если мы хотим взаимной однозначности, мы должны потребовать, чтобы
$v \ne \dfrac {\overrightarrow{PQ}}{|\overrightarrow{PQ}|}$ ни для каких различных точек
$P,Q\in M$.

Чтобы переформулировать это требование на более удобном нам языке,
рассмотрим множество $Y$ пар $(P, Q)$, где $P, Q \in M$, $P \ne Q$.
Другими словами, $Y = (M \times M)\backslash \Delta$, где $\Delta$~---
диагональ в декартовом произведении $M \times M$, т.е. множество пар $(P,
P)$.
\begin{zad}
Доказать, что $Y$ --- гладкое $2n$--мерное многообразие.
\end{zad}

Рассмотрим гладкое отображение:
$$ g :  Y \rightarrow S^{N-1},
$$
где $g(P, Q) = \dfrac {\overrightarrow{PQ}}{|\overrightarrow{PQ}|}$.

Условие взаимной однозначности проекции $\pi$ можно
переформулировать так: $v$ не содержится в образе отображения $g :  Y
\rightarrow S^{N-1}$.

Итак, проекция $\pi: M \rightarrow \Bbb R^{N-1}$ является взаимно-однозначным
погружением (т.е. вложением) тогда и только тогда, когда
$v$ не принадлежит множеству $f(X) \cup g(Y) \subset S^{N-1}$.

Теперь нам нужно разобраться с тем, как устроены множества $f(X)$ и $g(Y)$.
Это гладкие образы $(2n-1)$--мерного и $2n$--мерного многообразия в
($N-1$)--мерной сфере. Интуитивно ясно, что если $2n < N -1$, то эти
множества представляют собой нечто, ``имеющее размерность, меньшую
размерности сферы'', и потому заведомо ее не покрывающее.

Для того, чтобы аккуратно сформулировать и доказать соответствующее
утверждение, нам понадобится понятие множества меры нуль в гладком
многообразии.
\begin{deff}
Пусть $M$ --- гладкое $n$-мерное многообразие, допускающее счетный атлас.
Множество $X\subset M$ имеет меру нуль в $M$, если для любой карты $U,\varphi$
счетного атласа множество $\varphi(X\cap U)$ имеет меру нуль в $\Bbb R^n$.
\end{deff}
\begin{zad}
Доказать, что это определение не зависит от выбора счетного атласа.
\end{zad}
\begin{zam}
Приведенное определение совершенно естественно: в каждой карте множество
$X$ можно понимать как подмножество в $\Bbb R^n$; если все эти множества
имеют меру нуль, то и все $X$ (в силу счетности числа карт) тоже должно
иметь \mbox{меру нуль.\quad$\blacksquare$}
\end{zam}
Докажем теперь нужное нам утверждение.
\begin{lemma}
Пусть $f: M \rightarrow N$ --- гладкое отображение гладких
многообразий и $\dim M < \dim N$, и многообразие $M$ допускает счетный
атлас. Тогда множество $f(M)$ имеет меру нуль в $N$.
\end{lemma}
\begin{proof}
Лемму достаточно доказать в одной карте,
поэтому можно считать, что $M=\Bbb R^m$, $N=\Bbb R^n$ и $m < n$.
Представим пространство $\Bbb R^m$ в виде объединения счетного числа
единичных кубов. Достаточно доказать утверждение для каждого куба, т.е.
показать, что множество $f(C)$ имеет меру нуль, где $C$ --- единичный куб.

Поскольку куб компактен, и $f$ --- гладкое, то существует константа $с$
такая, что:
$$ |f(x) - f(y)| < c |x - y|.  $$
Отсюда следует, что для любого $m$-мерного куба со стороной $a$, содержащегося в $C$,
его образ можно покрыть $n$-мерным кубом со стороной $c_1a$.
Разобьем каждую сторону куба $C$ на $k$ равных частей; при этом
весь куб разобьется на $k^m$ маленьких кубиков \mbox{со стороной
$1/k$}. Образ куба $C$ покрывается образами этих кубиков, причем каждый образ содержится в
$n$--мерном кубике со стороной $c_1/k$. Итак, множество $f(C)$ покрывается
$k^m$ $n$-мерными кубами со стороной $c_1/k$. Подсчитаем
суммарную меру этих кубов. Очевидно, она равна:
$$
k^m(\frac{c_1}{k})^n=c_1^n k^{m-n}.
$$
Устремим теперь $k$ к бесконечности; поскольку $m <
n$, множество $f(C)$ можно покрыть кубами сколь угодно малой суммарной
меры.  Следовательно, это множество имеет меру нуль. Лемма
доказана.
\end{proof}
Из доказанной леммы  немедленно следует утверждение теоремы Уитни.
Действительно, при условии $2n < N-1$ множества $f(X)$ и $g(Y)$ имеют меру
нуль в сфере $S^{N-1}$.  Поэтому найдется $v \in S^{N-1}$, не попадающий в
$f(X) \cup g(Y)$; построенная по такому вектору тогда проекция $\pi$
является вложением.
\end{proof}
\begin{zam}
Если от отображения $f$ требовать, чтобы оно было лишь погружением,
множество $Y$, рассматриваемое при доказательстве теоремы Уитни, можно
исключить из рассмотрения.  Пользуясь тем, что множество $X$ имеет
размерность, на единицу меньшую ($2n-1$), немедленно приходим к
следующему утверждению. Любое гладкое компактное $n$-мерное многообразие можно
погрузить в евклидово пространство размерности $2n$.\quad$\blacksquare$
\end{zam}
\begin{zad}
Построить вложение проективной плоскости в $\Bbb R^5$ и погружение в $\Bbb R^3$.
\end{zad}

